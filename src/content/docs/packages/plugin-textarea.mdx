---
title: "@vimee/plugin-textarea"
description: Simple textarea-based Vim editor
sidebar:
  order: 3
---

A lightweight Vim editor plugin that uses a standard HTML `<textarea>` as the editing surface. Ideal for simple use cases where syntax highlighting is not needed.

## Installation

```bash
npm install @vimee/core @vimee/plugin-textarea
```
